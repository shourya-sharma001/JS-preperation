const myNumers = [1,2,3,4]

const newnums = myNumers.map( (num) => num + 10);

console.log(newnums)

// ============================================================

                        // CHAINING

                        // ************

//     const newNums = myNumers
//                     .map((num) => num * 10)
//                     .map((num) => num + 1)
//                     .filter((num) => num > 40)

// console.log(newNums);


