const name = "shourya";

const repoCount = 50;

// console.log(name + repoCount + " Value");

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`)

const gameName = new String('Shouryassss') // Copy paste this in the console pannel of the browser and see the result.

console.log(gameName[0]); // Used to access the string
console.log(gameName.__proto__); // Another method used to access the string. All the methods/ functions to access and play with the string are stored in the prototypes only.


console.log(gameName.length);
console.log(gameName.toUpperCase()); //Turns all characters to upper case.
console.log(gameName.charAt(5));// Tells the value at the provided index.

console.log(gameName.indexOf('a'));

// ****************** NOTE ********************

// See the other string methods in the console pannel of the browser in prototype and make the  notes out of it.